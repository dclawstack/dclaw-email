apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "dclaw-email.fullname" . }}
  labels:
    {{- include "dclaw-email.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "dclaw-email.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "dclaw-email.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "dclaw-email.serviceAccountName" . }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8110
              protocol: TCP
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
